<!DOCTYPE html>
<html>
  <head>
    <title>ShinLearn2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div>
      <% if user_signed_in? %>
        <%= button_to "ログアウト", '/logout', method: :delete, data: { turbo_confirm: '本当にログアウトしますか？' }%>
      <% else %>
        <%= button_to "ログイン", '/auth/keycloak', data: {turbo: false} %>
        <%= link_to "新規会員登録", new_user_path, data: {turbo: false} %>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
